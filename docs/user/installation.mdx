---
title: "Installation"
description: "Get ichat running in 30 seconds"
---

## Quick Start

Choose your preferred installation method:

<CardGroup cols={2}>
  <Card title="Docker" icon="docker" href="#docker-recommended">
    Easiest setup, works everywhere
  </Card>
  <Card title="Native Binary" icon="download" href="#native-binary">
    Flexible deployment
  </Card>
</CardGroup>

## Docker (Recommended)

The fastest way to get started. No dependencies, no configuration needed.

### Basic Setup

```bash
docker run -it --rm \
  -e API_KEY="<YOUR_OPENROUTER_KEY>" \
  -p 80:80 \
  -v "$(pwd)/data:/data" \
  ghcr.io/isomoes/ichat-web:latest
```

<Note>
  Replace `<YOUR_OPENROUTER_KEY>` with your actual API key from [OpenRouter](https://openrouter.ai)
</Note>

### What This Does

- Downloads the latest ichat image (~17MB)
- Starts the server on port 80
- Creates a `data` folder for persistent storage
- Sets your API key for LLM access

### Access Your Instance

Open your browser and navigate to:
- **Local**: http://localhost
- **Network**: http://YOUR_IP_ADDRESS

**Default credentials:**
- Username: `admin`
- Password: `P@88w0rd`

<Warning>
  Change the default password immediately after first login!
</Warning>

### Persistent Storage

To keep your conversations and settings:

```bash
docker run -d \
  --name ichat \
  -e API_KEY="your-key-here" \
  -p 80:80 \
  -v ./ichat-data:/data \
  --restart unless-stopped \
  ghcr.io/isomoes/ichat-web:latest
```

### Using Nightly Builds

For the latest features (may be unstable):

```bash
docker run -p 80:80 \
  -e API_KEY="your-key-here" \
  -v ./data:/data \
  ghcr.io/isomoes/ichat-web:nightly
```

## Native Binary

For maximum performance and minimal resource usage.

### Download

Get the latest release for your platform:

**Linux (x86_64):**
```bash
wget https://github.com/isomoes/ichat-web/releases/download/latest/ichat-x86_64-unknown-linux-gnu
chmod +x ichat-x86_64-unknown-linux-gnu
```

**Linux (ARM64/aarch64):**
```bash
wget https://github.com/isomoes/ichat-web/releases/download/latest/ichat-aarch64-unknown-linux-gnu
chmod +x ichat-aarch64-unknown-linux-gnu
```

**Windows:**
```bash
# Download from GitHub Releases page
https://github.com/isomoes/ichat-web/releases/download/latest/ichat-x86_64-pc-windows-msvc.exe
```

### Run

```bash
# Set API key
export API_KEY="your-openrouter-key"

# Run ichat
./ichat-x86_64-unknown-linux-gnu
```

## Platform-Specific Notes

<Accordion title="Raspberry Pi">

  **Installation:**
  ```bash
  # Use ARM64 binary
  wget https://github.com/isomoes/ichat-web/releases/download/latest/ichat-aarch64-unknown-linux-gnu
  chmod +x ichat-aarch64-unknown-linux-gnu
  ./ichat-aarch64-unknown-linux-gnu
  ```
</Accordion>

<Accordion title="Windows">
  **Download:** Get the `.exe` from [releases](https://github.com/isomoes/ichat-web/releases)
  
  **Run:**
  ```powershell
  # PowerShell
  $env:API_KEY="your-key"
  .\ichat.exe
  ```
  
  **Firewall:** You may need to allow ichat through Windows Firewall
</Accordion>

## Troubleshooting

<Accordion title="Port 80 already in use">
  Use a different port:
  ```bash
  docker run -p 8080:80 ... ghcr.io/isomoes/ichat-web:latest
  ```
  Access at http://localhost:8080
</Accordion>

<Accordion title="Unable to connect 80">
    Docker build has default port 80.

    Other deployment option has default port 3000, and bind only to local interface
    (You cannot access ichat from other device).
</Accordion>

<Accordion title="Can't connect from network">
  - Check firewall settings
  - Ensure using `0.0.0.0:80` not `127.0.0.1:80`
  - Verify correct IP address
</Accordion>
